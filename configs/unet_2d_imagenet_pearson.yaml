base: base.yaml
experiment_name: unet_2d_imagenet_pearson

model:
  dims: 2d
  encoder_weights: imagenet
  time_kernel: null

training:
  batch_size: 8
  losses:
    pearson: 1.0
  checkpoint_dir: ckpts/unet_2d_imagenet_pearson
